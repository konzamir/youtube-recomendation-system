apiVersion: v1
kind: Service
metadata:
  name: {{ include "youmed.fullname" . }}
  labels:
    {{- include "youmed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "youmed.selectorLabels" . | nindent 4 }}
