apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "youmed.fullname" . }}-test-connection"
  labels:
    {{- include "youmed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "youmed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
